# Local .terraform directories
**/.terraform/*

# Local .infracost directories
**/.infracost/*

# Local .vs directories
**/.vs/*

# .tfstate files
*.tfstate
*.tfstate.*

# Ignore the dependency lock file generated automatically for each Terraform init
.terraform.lock.hcl

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run 
# Most .tfvars files are managed as part of configuration and so should be included in version control
*.auto.tfvars

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json